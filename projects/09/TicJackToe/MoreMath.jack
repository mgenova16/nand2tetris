class MoreMath {

    function int mod(int numer, int denom) {
        var int div;
        var int result;

        if (numer < 0) {
            let numer = numer + denom;
        }
        let div = numer / denom;
        let result = numer - (div * denom);
        return result;
    }
}
